import { NewTeamScreen } from "@/features/onboarding/new-team-screen";

/** 00 팀 만들기 — 초대 링크를 받은 사람 화면만 있고, 처음 만드는 사람 화면이 없던 것을 보완한 화면. */
export default function NewTeamPage() {
  return <NewTeamScreen />;
}
