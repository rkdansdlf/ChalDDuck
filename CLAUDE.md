@AGENTS.md

# 찰떡 — 작업 규칙

디자인 핸드오프(33화면 프로토타입)를 실제 코드베이스로 옮기는 프로젝트입니다.
**원본 자료는 [`docs/handoff/`](docs/handoff) 에 그대로 보관돼 있습니다** — 화면을 만들기 전에
`HANDOFF.md` 의 해당 화면 설명과 `prototype/` 의 원본 JSX 를 먼저 읽으세요.
`docs/handoff/` 는 빌드 대상이 아니고 lint 에서도 제외돼 있습니다. 고치지 마세요.

## 색 이름에 `x-`/`y-` 를 쓰지 않는다

Tailwind 는 `border-y-*`, `border-x-*` 를 **축 border-width** 유틸리티로 먼저 해석합니다.
색 토큰을 `y-600` 으로 이름 지으면 `border-y-600` 이 600px 테두리가 됩니다(실제로 겪은 버그).

- CSS 변수는 핸드오프 원본 이름 그대로 — `--y-400`, `--c-400`
- Tailwind 유틸리티 이름은 두 글자를 넘긴다 — `bg-yellow-400`, `text-coral-700`
- 매핑은 [`src/app/globals.css`](src/app/globals.css) 의 `@theme` 한 곳에만 있다

## 디자인 토큰은 확정값이다

[`src/styles/tokens/brand-warm.css`](src/styles/tokens/brand-warm.css) 의 값은 디자인팀이
대비(WCAG)까지 검증해 확정한 것입니다. 색이 안 맞아 보여도 **임의로 고치지 말고** 디자인팀에 확인하세요.
주석에 "왜 이 값인지"가 적힌 항목(`--y-700`, `--ink-400`)은 특히 그렇습니다.

화면 코드에 새 hex 값을 직접 적지 마세요. 필요하면 토큰에 이름을 붙여 추가합니다.

## 화면을 만들기 전에 `src/components/ui` 를 먼저 본다

공통 컴포넌트 17종이 이미 있습니다. 화면 안에서 비슷한 걸 새로 만들지 말고, 없으면 이 폴더에 추가하세요.
특히 이것들은 규칙이 붙어 있습니다.

- **`Chip` / `StatusBadge`** — 상태는 **색만으로 구분하지 않는다**. 항상 아이콘 + 텍스트를 함께 둔다.
  상태 어휘는 `STATUS` 한 곳에 모여 있다(드라이브·버전·할 일이 같은 색·아이콘을 쓴다). 화면에서 새 상태를 지어내지 말 것.
- **`Btn`** — 채운 버튼(primary/yellow)은 **한 화면에 하나**. 비활성은 투명도가 아니라 `off` 팔레트.
  `size="sm"` 은 44px 미만이라 보조 동작 전용.
- **`Panel` / `Rows`** — 연속된 항목은 카드를 겹겹이 쌓지 않고 `Rows`(한 면 + 구분선)로.
  표면은 테두리와 그림자를 **동시에 쓰지 않는다**(`SURFACE` 참고).
- **`Dock`** — 흐름 안의 형제 요소다. `Body` 가 알아서 줄어드니 여백 숫자를 맞춰 넣지 말 것.
  탭바 위에 띄워야 할 때만 `above` 를 준다.
- **`Icon`** — 쓰는 아이콘만 레지스트리에 등록한다. `import * as Lucide` 로 바꾸지 말 것(번들에 1000개가 들어온다).

타이포는 `t-h1`, `t-body`, `t-chip` 같은 유틸리티로 통일돼 있습니다(`globals.css`).
화면에 `text-[15px] leading-[1.62]` 를 흩뿌리는 대신 이름 있는 값을 쓰세요.

## MBTI 는 역할 배정에 쓰지 않는다

제품 약속입니다. MBTI 는 **캐릭터 발급과 소통 방식 이해에만** 씁니다.
역할은 희망·Veto·경험·가능한 시간으로만 조율합니다. 30초 컷은 정식 검사가 아니므로
화면 문구에서 "진단"·"정확한 결과"라고 부르지 않습니다.

## 기획안에 없는 규칙은 지어내지 않는다

값이 없으면 `null` 로 두고, 화면에서는 `<Undecided>` 로 표시합니다.
이 박스는 사용자에게 보이지 않고 검토 모드(`?review=1`)에서만 나타납니다.
확정이 필요한 정책 10건은 `docs/handoff/HANDOFF.md` 의 "확정되지 않은 정책" 표에 있습니다.

## 데이터는 `src/data/api.ts` 로만 읽는다

화면에서 `MOCK_*` 를 직접 import 하지 마세요. 모든 함수가 `async` 인 것도
나중에 `fetch` 로 갈아끼울 때 호출부를 안 고치기 위해서입니다.

## 검사

작업을 마치기 전에:

```bash
npm run build && npx tsc --noEmit && npm run lint
```

레이아웃은 눈으로 확인하세요. 타입 검사가 통과해도 Tailwind 클래스 충돌은 잡히지 않습니다
(위의 600px 테두리 버그가 그랬습니다).
